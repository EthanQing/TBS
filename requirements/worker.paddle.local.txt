-r worker.common.txt

# Local (non-Docker) helpers for Paddle worker development.
# Install one Paddle runtime manually (CPU or GPU):
#   pip install paddlepaddle==2.6.2
#   pip install paddlepaddle-gpu==2.6.2
#
# PaddleDetection is loaded from a release/2.6 source checkout:
#   git clone --branch release/2.6 --depth 1 https://github.com/PaddlePaddle/PaddleDetection.git PaddleDetection
#   set PADDLE_DET_DIR=%CD%\PaddleDetection

pycocotools
typeguard
visualdl>=2.2.0
lap
tqdm
motmetrics
pyclipper
shapely
terminaltables
Cython
numpy==1.23.5
protobuf<=3.20.3
